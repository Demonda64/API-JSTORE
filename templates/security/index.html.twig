{% extends 'base.html.twig' %}

{% block title %}Formulaire d'inscription{% endblock %}

{% block body %}


<div class="container">
<h1> {{ controller_name }} 👍</h1>

{{ form_start(form) }}

        <div class="row">
                <div class="col-md-4">
                {{ form_row(form.username, {'label':"Nom d'utulisateur : ", 'attr':{
                'placeholder':"Votre nom d'utilisateur ..."
        }}) }}
                </div>
                <div class="col-md-4">
                {{ form_row(form.lastname, {'label':"Nom de famille : ", 'attr':{
                'placeholder':"Votre nom ..."
        }}) }}
                </div>
        </div>
        <div class="row">
                <div class="col-12">
                {{ form_row(form.email, {'label':"Email : ", 'attr':{
                'placeholder':"Votre email ..."
        }}) }}</div>
                <div class="col-12">
                {{ form_row(form.password, {'label':"Mot de Passe : ", 'attr':{
                'placeholder':"Votre mot de passe ..."
        }}) }}</div>
                <div class="col-12">
                {{ form_row(form.passwordConfirm, {'label':"Confirmation mot de passe : ", 'attr':{
                'placeholder':"Confirmation mot de passe..."
        }}) }}</div>
        </div>

        <button type="submit" class="btn btn-outline-primary">S'inscrire</button>
{{ form_end(form) }}
</div>
{% endblock %}
